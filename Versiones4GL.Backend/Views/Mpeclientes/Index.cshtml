@model IEnumerable<Versiones4GL.Common.Models.Mpecliente>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Version)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Version)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MpeclienteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MpeclienteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MpeclienteId })
        </td>
    </tr>
}

</table>
